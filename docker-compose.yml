services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    environment:
      NODE_ENV: production
    ports:
      - "127.0.0.1:${HOST_PORT:-3001}:80"
    restart: unless-stopped

networks:
  default:
    name: futurist-land-net

